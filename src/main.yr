mod main;
import std::io;

import balder::_;
import std::fs::_;

def main () {

    with dmut app = Application::new (Path::new ("config/app.toml"s8)) {
        app:.run ();
    } catch {
        bd : &BalderError => {
            println (bd);
        }
    }

} 
