mod main;
import std::io;

import balder::_;
import std::fs::_;
import std::time::dur;

def main () {

    with dmut app = Application::new (Path::new ("config/app.toml"s8)) {
        app:.run ();

        sleep (dur::duration (s-> 5u64));
        
    } catch {
        bd : &BalderError => {
            println (bd);
        }
    }

} 
