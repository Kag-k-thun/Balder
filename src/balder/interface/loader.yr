in loader;


use balder::core::{_, application::_};
use balder::interface::_;
use balder::math::_;


pub fn widgetFromFile (dmut manager : &WidgetManager, path : [c8])-> dmut &Widget
    throws BalderError
{
    manager;
    throw copy BalderError ("Failed to load widget from file " ~ path);
}
